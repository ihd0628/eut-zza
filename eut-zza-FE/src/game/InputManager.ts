/**
 * 게임 플레이 중 발생하는 키보드 입력을 수집하고 게임 엔진에 전달한다.
 *
 * React 컴포넌트의 onKeyDown에 게임 입력 처리를 직접 넣지 않고 분리하면,
 * 화면 UI의 렌더링과 게임 입력 처리를 독립적으로 관리할 수 있다.
 *
 * 이후 이 파일에는 다음 기능이 들어간다.
 * - D, F, J, K 키의 keydown / keyup 이벤트 감지
 * - 입력이 발생한 시점의 시간 기록
 * - 게임 엔진에 입력 이벤트를 전달하는 callback 등록
 * - 입력을 받기 시작하는 attach()와 이벤트를 해제하는 detach()
 * - 게임에만 필요한 키의 기본 브라우저 동작 방지 여부 관리
 *
 * 실제 노트 판정과 점수 계산은 GameEngine 또는 별도의 판정 모듈이 담당한다.
 */
