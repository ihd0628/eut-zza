# Eut-zza 에이전트 작업 지침

이 저장소에서 작업을 시작하기 전에 항상 루트의 `road-map.md`를 읽고, 프로젝트의 개발 및 학습 기준으로 사용한다. 제안하는 작업과 구현 결정은 현재 로드맵 단계의 학습 목표 및 완료 조건에 부합해야 한다.

Playwright를 사용한 브라우저 검증이 끝나면 해당 검증 과정에서 생성한 스냅샷, 스크린샷, 콘솔 로그, 트레이스 등 `.playwright-cli/` 또는 `output/playwright/` 아래의 임시 산출물을 삭제한다. 사용자가 보존을 요청한 산출물은 삭제하지 않는다.
